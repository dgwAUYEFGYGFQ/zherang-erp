# 采购云折让管理开发功能说明书

## 文档版本信息

| 版本 | 日期 | 文档状态 | 说明 |
| --- | --- | --- | --- |
| V1.0 | 2026-09-16 | 开发基线 | 根据已确认功能点、最终交互原型及 SAP 接口字段 Excel 整理 |

## 1 文档定位

### 1.1 建设性质

本需求采用“新增模块为主、现有页面局部改造为辅”的建设方式。

- 全新新增：折让管理菜单、计提单管理、计提单新增、计提单详情、计提单版本快照、结算单管理、结算单新增、结算单详情、结算单版本快照。
- 现有页面改造：采购云现有“对账单确认”页面的附加费用区域，将流水号改为从结算单列表中选择，并增加结算单占用与作废释放联动。
- 本文不把计提单、结算单描述成原有页面优化；这些页面均按全新功能开发。

### 1.2 需求依据与优先级

本文依据以下材料编制：

1. 用户最终确认的业务规则和状态规则。
2. 当前采购云折让管理交互原型。
3. 《正泰新能ERP再造项目_FICO146_采购折让计提数据同步接口.xlsx》中的计提、结算字段。

若原型 Mock 行为与最终确认规则不一致，以本文规则为准。例如：编辑稿保存不生成正式版本号；同步失败不进入版本记录；结算单列表不提供批量核对状态、批量导出或导出按钮。

### 1.3 使用角色

| 角色 | 主要职责 |
| --- | --- |
| 折让业务人员 | 新建、维护、提交计提单和结算单，查看 SAP 生效版本与历史快照，处理同步失败 |
| 对账业务人员 | 在对账单附加费用中选择结算单，确认占用；按现有对账单权限执行作废 |
| 系统 | 生成单号、校验月份唯一性、获取收货记录、计算金额、同步 SAP、维护版本快照和占用关系 |

## 2 功能范围

### 2.1 菜单与页面

| 一级菜单 | 二级菜单/入口 | 页面性质 | 说明 |
| --- | --- | --- | --- |
| 折让管理 | 计提单管理 | 新增 | 查询计提单并进入新增、详情、编辑稿、SAP 版本和历史快照 |
| 折让管理 | 结算单管理 | 新增 | 查询结算单并进入新增、详情、编辑稿、SAP 版本和历史快照 |
| 对账开票管理 | 入库对账单—入库单明细查询详情 | 改造 | 在现有“入库单附加费用信息”区域选择折让结算单，确认占用，作废时释放 |

### 2.2 功能清单

| 序号 | 功能点 | 功能页面 | 端口 |
| --- | --- | --- | --- |
| 1 | 新建、保存、提交计提单，修改后可再次提交并保留成功版本 | 采购端：折让管理—计提单管理 | 采购端 |
| 2 | 新建、保存、提交结算单；按计提年月关联计提单号；修改后可再次提交并保留成功版本 | 采购端：折让管理—结算单管理 | 采购端 |
| 3 | 入库对账单维护附加费用时，选择“折让（费用）”或“折让（物料）”后从流水号弹窗选择结算单，形成占用并随对账单作废释放 | 采购端：对账开票管理—入库单明细查询详情 | 采购端 |

### 2.3 不在本次范围

- 不新增或展示“SAP结算状态”“已结算/未结算”。采购云无法提前获知该状态，只处理 SAP 提交结果。
- 不提供“对比SAP版本”、字段差异计算、差异高亮或版本恢复。
- 不设计新的审批流。
- 不重做对账单确认页面的其他业务区域。
- 不提供计提单、结算单列表导出、批量导出或结算单批量核对状态。

## 3 整体业务逻辑

### 3.1 主流程

1. 折让业务人员新建计提单，选择计提年月、公司代码、供应商，品类固定为玻璃。
2. 系统按“计提年月 + 公司代码 + 供应商 + 玻璃”获取收货记录。
3. 用户在收货明细行维护折让后M²不含税单价，系统逐行计算折让金额和折后金额。
4. 用户保存后形成草稿并生成 A 单号；提交 SAP 成功后生成 V1 正式版本。
5. 用户新建结算单，选择结算年月和计提年月。系统通过计提年月自动找到唯一计提单号，仅建立关联，不复制计提单明细、比例或金额。
6. 系统按“结算年月 + 公司代码 + 供应商 + 玻璃”独立获取结算收货记录。用户在结算收货明细行维护折让后M²不含税单价并提交 SAP。
7. 对账业务人员在现有入库对账单的附加费用行选择“折让（费用）”或“折让（物料）”。系统弹出结算单选择框，只展示与当前对账单公司、供应商、工厂一致，且已同步 SAP、未占用的结算单。用户单选后，系统将结算单号带入流水号，将结算单折让金额带入不含税金额，并按税率计算含税金额。对账单确认后正式占用该结算单。
8. 被占用结算单保持只读；只有引用它的对账单作废成功后，系统才释放占用。

### 3.2 单据关系

| 关系 | 规则 |
| --- | --- |
| 计提单与收货记录 | 按计提年月、公司代码、供应商、玻璃品类独立获取 |
| 结算单与计提单 | 仅通过“计提年月 → 唯一计提单号”建立关系 |
| 结算单与收货记录 | 按结算年月、公司代码、供应商、玻璃品类独立获取，不继承计提单明细 |
| 入库对账单与结算单 | 附加费用行引用结算单最后一次 SAP 同步成功的正式版本；调整类型仅限“折让（费用）”或“折让（物料）”时启用该选择逻辑 |

## 4 公共业务规则

### 4.1 单号生成

| 单据 | 规则 | 示例 | 生成时点 |
| --- | --- | --- | --- |
| 计提单 | A + YYMMDD + 当日3位流水号 | A260916001 | 第一次保存或直接第一次提交时 |
| 结算单 | S + YYMMDD + 当日3位流水号 | S260916001 | 第一次保存或直接第一次提交时 |

单号由系统生成，用户不可编辑。同一自然日内流水号从 001 递增且不得重复。修改、重新保存、重新提交和正式版本升级均沿用原业务单号。

### 4.2 月份唯一性

| 单据 | 唯一键 | 校验时点 | 冲突处理 |
| --- | --- | --- | --- |
| 计提单 | 计提年月 | 选择年月时即时校验；保存/提交前再次校验 | 任意状态的同年月单据均阻断新建；不生成新 A 单号 |
| 结算单 | 结算年月 | 选择年月时即时校验；保存/提交前再次校验 | 任意状态的同年月单据均阻断新建；不生成新 S 单号 |

唯一性只看月份，不因公司、供应商或状态不同而放开。编辑原单据时排除当前单据本身。删除从未成功提交 SAP 的首次草稿后，对应月份重新可用。

### 4.3 收货记录获取

| 单据 | 获取条件 | 触发方式 | 结果处理 |
| --- | --- | --- | --- |
| 计提单 | 计提年月 + 公司代码 + 供应商 + 品类=玻璃 | 用户点击“获取最新收货记录” | 用最新结果刷新当前编辑稿明细并重新汇总金额 |
| 结算单 | 结算年月 + 公司代码 + 供应商 + 品类=玻璃 | 用户点击“获取最新收货记录” | 独立查询结算收货记录，不读取或复制关联计提单明细 |

若关键条件缺失，系统不发起取数并提示用户补全。再次获取采用刷新/覆盖口径，不重复追加同一业务行。历史快照和 SAP 生效版本不可获取最新记录。

### 4.4 折让计算（行级折后单价）

计提单、结算单均不再维护“折让比例”。折让后M²不含税单价在收货明细行维护：支持勾选多行后批量赋值，也支持逐行直接编辑；结算单不从关联计提单带入该值。

- 原M²不含税单价 = 原金额 ÷ 玻璃面积；面积为空或0且原金额为0时显示 0.0000。
- 折后金额 = 玻璃面积 × 折让后M²不含税单价。
- 折让金额 = 原金额 − 折后金额；允许负数折让金额。
- 原M²不含税单价、折让后M²不含税单价、行原金额、行折后金额、行折让金额、头部金额合计均四舍五入保留4位小数。
- 计算顺序：每行先计算并保留4位，再汇总各行已保留4位的金额；头部合计再次保留4位。
- 折让后M²不含税单价允许为0或负数；输入精度最多4位小数。
- 面积为空或0且原金额不为0，或面积不为0且原金额为空：阻断保存和提交，提示“该明细缺少有效玻璃面积/原金额，无法计算折后金额”。
- 面积与原金额均为0：允许保留该行，原M²不含税单价显示0.0000。
- 获取最新收货记录后，按当前单据各行维护的单价重新计算；刷新时保留业务行已有单价，新出现的行单价为空，必须补齐后才能保存或提交。
- SAP报文按行传输“折让后M²不含税单价”，同一行传输该行维护值；采购云头部仅保存汇总金额，不再保存头部统一单价。

### 4.5 未保存修改提示

“有未保存修改”是页面临时提示，不是第五种同步状态。

| 场景 | 页面显示 | 列表数据 | 离开页面 |
| --- | --- | --- | --- |
| 已同步单据在页面中被修改但未保存 | 继续显示原同步状态，同时显示“有未保存修改”提示 | 仍显示上一次已保存的数据 | 弹出确认，提示未保存内容将丢失 |
| 用户点击保存 | 根据是否存在 SAP 成功版本转为“草稿”或“待重新提交” | 显示最新保存数据 | 可正常离开 |

因此不会出现“页面看起来已提交，但列表已经混入未保存内容”的情况：详情页内的临时编辑值只存在于当前会话，列表始终读取最新已保存版本。

## 5 同步状态与版本规则

### 5.1 同步状态定义

计提单和结算单统一使用四种状态。

| 状态 | 判定条件 | 是否存在编辑稿 | SAP生效版本 |
| --- | --- | --- | --- |
| 草稿 | 单据已保存，但从未成功提交 SAP | 是 | 无 |
| 已同步 | 当前采购云已保存内容与最后一次 SAP 成功版本一致 | 否 | 有，至少 V1 |
| 待重新提交 | 单据曾成功提交 SAP，之后又修改并保存 | 是 | 仍为上一次成功版本 |
| 同步失败 | 最近一次提交 SAP 返回失败 | 是 | 保持原成功版本；若从未成功则仍无版本 |

### 5.2 状态流转

| 当前状态/场景 | 用户动作 | SAP结果 | 新状态 | 版本处理 |
| --- | --- | --- | --- | --- |
| 未保存新单 | 保存 | 不调用 | 草稿 | 不生成正式版本 |
| 未保存新单/草稿 | 提交 SAP | 成功 | 已同步 | 生成 V1 |
| 未保存新单/草稿 | 提交 SAP | 失败 | 同步失败 | 不生成 V1，保留编辑稿 |
| 已同步 | 页面修改但未保存 | 不调用 | 已同步，同时提示有未保存修改 | 版本不变 |
| 已同步 | 保存修改 | 不调用 | 待重新提交 | 仍基于当前 SAP 版本，不预生成下一版本 |
| 待重新提交/同步失败 | 提交 SAP | 成功 | 已同步 | 生成当前最大成功版本的下一版本 |
| 待重新提交/同步失败 | 提交 SAP | 失败 | 同步失败 | 不生成版本，SAP 生效版本不变 |
| 同步失败 | 修改后再次保存，且已有成功版本 | 不调用 | 待重新提交 | 保留上次失败原因供追溯 |
| 同步失败 | 修改后再次保存，且无成功版本 | 不调用 | 草稿 | 保留上次失败原因供追溯 |

### 5.3 正式版本号

- 正式版本从 V1 开始，只在 SAP 返回成功后生成。
- 后续成功版本按 V2、V3 顺序递增。
- 保存编辑稿不分配正式版本号，只记录“基于 Vn 的当前编辑稿”。
- 提交失败不占用版本号、不进入版本记录；下一次真正成功时仍生成“最大成功版本 + 1”。
- SAP 生效版本始终指最后一次成功同步的版本。

### 5.4 版本记录与完整快照

每个成功版本必须保存当次提交成功时的完整快照，至少包括：头信息、折让后M²不含税单价、汇总金额、全部收货明细、提交人、提交成功时间和 SAP 成功结果。打开历史版本时必须读取该版本自己的快照，不得按当前比例重算旧版本，也不得读取当前最新明细替代旧明细。

版本记录只展示成功版本，字段顺序固定为：版本、状态、折让后M²不含税单价、折让金额、折后金额、提交人、提交时间、SAP结果、操作。操作列仅提供“查看完整快照”。

### 5.5 历史快照返回规则

计提单和结算单完全一致。

| 当前单据情况 | 在历史快照点击“返回”后的目标 |
| --- | --- |
| 存在尚未成功提交 SAP 的编辑稿，包括草稿、待重新提交、同步失败或实际存在未提交编辑稿 | 返回当前最新编辑稿详情，展示用户最新保存的数据 |
| 不存在编辑稿 | 返回最新一次 SAP 成功版本详情 |

历史快照中的“返回”只改变查看目标，不修改任何业务数据。

### 5.6 删除编辑稿

| 场景 | 按钮文案 | 处理结果 |
| --- | --- | --- |
| 已存在 SAP 成功版本，另有当前编辑稿 | 删除编辑稿 | 删除未提交修改，恢复最后一次 SAP 成功版本；同步状态恢复“已同步”；历史版本不变 |
| 从未成功提交 SAP，仅有首次草稿 | 删除草稿/删除 | 删除整张业务单据并释放月份唯一性；单据从列表消失 |
| 同步失败且存在历史成功版本 | 删除编辑稿 | 删除失败编辑稿并恢复最后一次 SAP 成功版本 |
| 已占用结算单且存在编辑稿 | 删除编辑稿 | 允许删除并恢复被占用的 SAP 成功版本；占用关系不变，单据继续只读 |

删除操作必须二次确认。删除编辑稿不删除正式版本；删除首次草稿属于整单删除。

## 6 SAP 提交规则

### 6.1 提交前校验

系统至少校验：必填头字段完整、月份唯一、存在收货明细、每条收货明细均已维护折让后M²不含税单价，且数值允许为0或负数、金额已计算、结算单已关联有效计提单、结算单未被对账单占用、当前页面无未保存修改。

### 6.2 返回处理

| SAP返回 | 采购云处理 |
| --- | --- |
| 成功 | 更新同步状态为已同步；生成下一正式版本；保存完整快照；更新 SAP 生效版本、同步人和同步时间 |
| 普通接口失败 | 更新为同步失败；保留编辑稿；不生成版本；展示 SAP/接口返回原因 |
| SAP已完成后续结算，不允许修改 | 更新为同步失败；保留编辑稿；原 SAP 成功版本不变；展示明确原因，例如“同步失败：该业务数据已在SAP完成结算，不允许修改。” |

采购云不提前维护或判断“SAP结算状态”，也不因推测 SAP 已结算而锁定计提单。计提单是否允许更新由提交时的 SAP 返回决定。结算单另受采购云对账占用规则约束。

## 7 结算单占用规则

### 7.1 占用状态

| 状态 | 含义 | 占用对账单号 |
| --- | --- | --- |
| 未占用 | 尚未被已确认对账单引用 | 空 |
| 已占用 | 已被一张有效对账单引用 | 必填，记录对账单号 |

### 7.2 占用与释放

1. 入库对账单待确认时，用户在附加费用行选择“折让（费用）”或“折让（物料）”，系统立即打开结算单选择框。
2. 候选结算单必须同时满足：公司与当前入库对账单一致、供应商一致、结算单收货明细工厂与当前应用工厂一致、同步状态=已同步、占用状态=未占用。
3. 用户只能单选。选择后，系统将结算单号回填到“流水号”，将该结算单当前 SAP 生效版本的折让金额回填到“不含税金额”，并按“含税金额 = 不含税金额 ×（1 + 税率）”计算含税金额，金额保留两位小数并保留正负号。此时只形成待确认引用，尚未正式占用。
4. 对账单保存并确认时，系统再次校验可选条件，并以不可分割的方式写入：结算单占用状态=已占用、占用对账单号=当前对账单号、对账单引用的结算单版本=当时最后一个 SAP 成功版本。
5. 已占用结算单不能被其他对账单再次选择。
6. 用户不能通过清空流水号、编辑结算单或删除编辑稿解除占用。
7. 只有引用该结算单的原对账单作废成功后，系统才将占用状态改为未占用并清空占用对账单号。

### 7.3 已占用结算单权限

| 操作 | 无编辑稿 | 存在未提交编辑稿 |
| --- | --- | --- |
| 查看当前正式版本 | 允许 | 允许 |
| 查看被冻结编辑稿 | 不适用 | 允许，只读 |
| 修改折让后M²不含税单价 | 禁止 | 禁止 |
| 获取最新收货记录 | 禁止 | 禁止 |
| 保存编辑稿 | 禁止 | 禁止 |
| 提交 SAP | 禁止 | 禁止 |
| 删除编辑稿 | 不适用 | 允许 |
| 查看历史版本 | 允许 | 允许 |

禁用操作提示统一说明占用原因，例如：“该结算单已被对账单 DZ261110003 占用，不允许修改或重新提交。”

## 8 页面功能说明

本章中的“字段/组件名称”同时覆盖页面字段、表格列、按钮、页签、提示和弹窗。字段表均使用统一格式。

### 8.1 计提单管理列表页

页面用途：查询计提单并进入新增、详情或当前编辑稿。默认按更新时间倒序展示。列表中的折让后M²不含税单价和金额始终取采购云最新已保存内容；存在编辑稿时不显示旧 SAP 版本的比例。

| **区域/位置** | **字段/组件名称** | **字段类型/组件类型** | **说明/功能** | **备注** |
| --- | --- | --- | --- | --- |
| 查询区 | 关键字 | 输入框 | 按计提单号、供应商、公司代码模糊查询 | 可清空 |
| 查询区 | 计提年月 | 月份选择器 | 按计提年月筛选 | 格式 YYYY-MM |
| 查询区 | 品类 | 下拉框 | 按品类筛选 | 当前仅玻璃 |
| 查询区 | 同步状态 | 下拉框 | 按草稿、已同步、待重新提交、同步失败筛选 | 不含 SAP 结算状态 |
| 查询区 | 重置 | 按钮 | 清空查询条件并恢复默认列表 | 始终可用 |
| 操作区 | 新增计提单 | 主按钮 | 进入新增计提单页 | 有新增权限时显示 |
| 列表 | 勾选框 | 多选框 | 选择列表行 | 本期无批量业务按钮，可保留通用表格选择能力，也可按开发规范取消 |
| 列表 | 计提单号 | 文本链接 | 展示 A 单号；点击进入详情页 | 固定左侧 |
| 列表 | 计提年月 | 月份文本 | 展示计提所属年月 | 唯一性字段 |
| 列表 | 公司代码 | 文本 | 展示公司代码 |  |
| 列表 | 供应商 | 文本 | 展示供应商名称 | 超长省略，悬停显示全称 |
| 列表 | 品类 | 文本 | 展示玻璃 |  |
| 列表 | 原金额 | 金额 | 展示最新已保存明细原金额合计 | 两位小数 |
| 列表 | 折让后M²不含税单价 | 金额 | 展示最新已保存明细行单价（多行以“—”表示） | 有编辑稿时取编辑稿明细行单价 |
| 列表 | 折让计提金额 | 金额 | 展示最新已保存折让计提金额合计 | 两位小数 |
| 列表 | 折后金额 | 金额 | 展示最新已保存折后金额合计 | 两位小数 |
| 列表 | 同步状态 | 状态标签 | 展示四种统一同步状态 | 同步失败时可提示 SAP 生效版本未变化 |
| 列表 | SAP生效版本 | 文本 | 展示最后一次 SAP 成功版本 | 无成功版本显示“—” |
| 列表 | 更新时间 | 日期时间 | 展示最新保存或成功同步时间 | YYYY-MM-DD HH:mm:ss |
| 列表操作 | 查看详情 | 文字按钮 | 进入单据详情，默认打开编辑稿；无编辑稿时打开 SAP 生效版本 | 始终可用 |
| 列表操作 | 编辑稿 | 文字按钮 | 进入当前编辑稿 | 无编辑稿时可进入详情并创建编辑稿视图；不得打开历史版本 |
| 表格底部 | 记录数/已选数 | 文本 | 展示查询结果数量和选择数量 |  |
| 表格底部 | 分页 | 分页器 | 切换页码和每页条数 | 默认每页10条 |

按钮规则：本页不显示导出、批量导出、“批量核对状态”或 SAP 版本对比入口。

### 8.2 新增计提单页

页面用途：建立玻璃折让计提单，获取收货记录并保存草稿或直接提交 SAP。

| **区域/位置** | **字段/组件名称** | **字段类型/组件类型** | **说明/功能** | **备注** |
| --- | --- | --- | --- | --- |
| 顶部 | 返回计提单管理 | 文字按钮 | 返回计提单列表 | 有未保存内容时提示确认 |
| 顶部 | 保存草稿 | 按钮 | 校验通过后保存采购云草稿；首次保存生成 A 单号 | 不生成正式版本 |
| 顶部 | 提交并同步SAP | 主按钮 | 校验并提交 SAP；可在未保存新单上直接提交，系统先生成单号 | 成功生成 V1；失败保留草稿 |
| 顶部 | 返回 | 按钮 | 返回列表 | 同返回计提单管理 |
| 标题区 | 处理状态 | 状态标签 | 未保存时显示待保存；保存后显示草稿；成功后显示已同步 | 页面提示，不增加状态枚举 |
| 基本信息 | 计提单号 | 只读文本 | 首次保存或提交时自动生成 | A+YYMMDD+3位流水号 |
| 基本信息 | 同步状态 | 只读状态 | 展示草稿/已同步/同步失败 | 未持久化前可显示“待保存” |
| 基本信息 | 品类 | 只读文本 | 固定为玻璃 | 不允许修改 |
| 基本信息 | 计提年月 | 月份选择器 | 计提业务月份 | 必填；选择时校验全局唯一性 |
| 基本信息 | 公司代码 | 下拉框 | 选择所属公司 | 必填；参与收货取数 |
| 基本信息 | 供应商 | 可搜索下拉框 | 选择单据供应商 | 必填；一张单内明细必须同供应商 |
| 基本信息 | 创建日期 | 只读日期 | 首次生成单据时记录 | YYYY-MM-DD |
| 基本信息 | 保存日期 | 只读日期 | 每次成功保存时更新 | YYYY-MM-DD |
| 基本信息 | 货币码 | 下拉框 | 选择 CNY、USD 等货币 | 必填；所有金额同币种 |
| 基本信息 | 收货记录数 | 只读数字 | 展示当前明细条数 | 取数后更新 |
| 折让计算 | 原金额 | 只读金额 | 汇总明细金额 | 两位小数 |
| 折让计算 | 折让计提金额 | 只读金额 | 汇总逐行折让计提金额 | 两位小数 |
| 折让计算 | 折后金额 | 只读金额 | 汇总逐行折后金额 | 两位小数 |
| 折让计算 | 备注 | 多行文本 | 记录本次计提说明 | 非必填，最多200字 |
| 行信息 | 收货明细 | 页签 | 展示本单收货记录 | 默认页签 |
| 收货明细顶部 | 匹配条件 | 提示文本 | 展示计提年月+公司代码+供应商+玻璃 | 用于让用户确认取数口径 |
| 收货明细顶部 | 最近获取时间 | 只读文本 | 展示最近一次成功取数时间和条数 | 无数据时显示尚未获取 |
| 收货明细顶部 | 获取最新收货记录 | 按钮 | 按计提条件刷新明细 | 必填条件不完整时阻断 |
| 收货明细表 | 物料凭证 | 文本 | 收货物料凭证号 | 接口行字段 |
| 收货明细表 | 行项目 | 文本 | 物料凭证行号 | 与物料凭证共同标识业务行 |
| 收货明细表 | 入库日期 | 日期 | 收货入库日期 |  |
| 收货明细表 | 采购订单 | 文本 | 采购订单号 |  |
| 收货明细表 | 采购订单行 | 文本 | 采购订单行号 |  |
| 收货明细表 | 工厂 | 文本 | 收货工厂 |  |
| 收货明细表 | 物料编码 | 文本 | 物料编码 | 对应 Excel“物料” |
| 收货明细表 | 物料名称 | 文本 | 物料描述 | 页面辅助字段，不替代物料编码 |
| 收货明细表 | 数量 | 数值 | 收货数量 | 支持负数 |
| 收货明细表 | 单价 | 金额 | 收货单价 | 按业务精度展示 |
| 收货明细表 | 面积 | 数值 | 收货面积 | 支持负数 |
| 收货明细表 | M²不含税单价 | 金额 | 每平方米不含税单价 | 原型展示4位小数 |
| 收货明细表 | 金额/原金额 | 金额 | 计算折让的基础金额 | 支持负数，保留两位 |
| 收货明细表 | 折让计提金额 | 金额 | 面积×该行折让后M²不含税单价 | 只读，实时计算 |
| 收货明细表 | 折后金额 | 金额 | 行金额-折让计提金额 | 只读，实时计算 |

校验与提示：

- 已存在同计提年月时立即提示现有计提单号，并禁用保存和提交。
- 未获取收货记录时不可保存或提交。
- 修改取数条件后清空原明细，要求重新获取，避免条件与明细不一致。
- 同一批最新记录再次获取时刷新结果，不重复追加。

### 8.3 计提单详情页

页面用途：维护当前编辑稿、查看 SAP 生效版本、查看成功版本记录。页面为完整详情页，不使用侧边栏。

| **区域/位置** | **字段/组件名称** | **字段类型/组件类型** | **说明/功能** | **备注** |
| --- | --- | --- | --- | --- |
| 顶部 | 返回计提单管理 | 文字按钮 | 返回计提单列表 | 有未保存修改时提示 |
| 顶部 | 当前编辑稿 | 视图切换按钮 | 切换到当前编辑稿 | 无编辑稿时可显示“新建编辑稿”入口 |
| 顶部 | SAP生效 Vn | 视图切换按钮 | 切换到最后一次 SAP 成功版本 | 全部只读 |
| 顶部 | 保存编辑稿 | 按钮 | 保存当前修改 | 仅编辑稿视图可用 |
| 顶部 | 提交SAP | 主按钮 | 提交已保存编辑稿 | 有未保存修改时禁用并提示先保存 |
| 顶部 | 删除编辑稿/删除草稿 | 危险按钮 | 按第5.6节执行 | 仅存在编辑稿或首次草稿时显示 |
| 顶部 | 返回 | 按钮 | 返回列表 |  |
| 页面提示 | 有未保存修改 | 警告提示 | 告知当前页面值尚未保存 | 不改变同步状态 |
| 页面提示 | SAP失败原因 | 错误提示 | 展示最近一次 SAP 失败的明确原因 | 同步失败时显示 |
| 基本信息 | 计提单号 | 只读文本 | 展示业务单号 |  |
| 基本信息 | 同步状态 | 只读状态 | 展示当前已保存内容的同步状态 | 四种状态 |
| 基本信息 | 品类 | 只读文本 | 玻璃 |  |
| 基本信息 | 公司代码 | 只读文本 | 展示取数公司 | 详情中不修改关键取数条件 |
| 基本信息 | 供应商 | 只读文本 | 展示供应商 |  |
| 基本信息 | 计提年月 | 只读月份 | 展示计提年月 |  |
| 基本信息 | 货币码 | 只读文本 | 展示货币 |  |
| 基本信息 | 创建日期 | 只读日期 | 首次生成日期 |  |
| 基本信息 | 保存日期 | 只读日期 | 最近保存日期 |  |
| 基本信息 | 编辑稿基础版本 | 只读文本 | 显示“基于Vn”；无正式版本时显示“无” | 不显示未成功的未来版本号 |
| 基本信息 | SAP生效版本 | 只读文本 | 最后一次成功版本 | 无成功版本显示“—” |
| 折让计算 | 原金额 | 只读金额 | 当前所选视图的原金额合计 |  |
| 折让计算 | 折让计提金额 | 只读金额 | 当前视图折让金额合计 |  |
| 折让计算 | 折后金额 | 只读金额 | 当前视图折后金额合计 |  |
| 折让计算 | 最近修改人/时间 | 只读文本 | 最近保存编辑稿的人员和时间 |  |
| 折让计算 | 最近同步人/时间 | 只读文本 | 最近 SAP 成功人员和时间 |  |
| 折让计算 | 备注 | 多行文本/只读文本 | 编辑稿视图可编辑；其他视图只读 | 最多200字 |
| 行信息 | 收货明细 | 页签 | 展示当前视图对应的完整明细 | 默认页签 |
| 收货明细顶部 | 取数条件与最近获取时间 | 提示文本 | 展示取数条件、比例、最近获取时间 |  |
| 收货明细顶部 | 获取最新收货记录 | 按钮 | 刷新编辑稿收货记录并重算 | 仅编辑稿视图显示/可用 |
| 收货明细表 | 物料凭证、行项目、入库日期 | 表格列 | 展示收货业务行标识和日期 | 只读 |
| 收货明细表 | 采购订单、采购订单行、工厂 | 表格列 | 展示采购来源和工厂 | 只读 |
| 收货明细表 | 物料编码、物料名称 | 表格列 | 展示物料信息 | 只读 |
| 收货明细表 | 数量、面积 | 数值列 | 展示数量和面积 | 支持负数 |
| 收货明细表 | 单价、M²不含税单价 | 金额列 | 展示价格 | 只读 |
| 收货明细表 | 金额、折让计提金额、折后金额 | 金额列 | 展示当前视图的行计算结果 | 两位小数 |
| 行信息 | 版本记录 | 页签 | 展示所有 SAP 成功正式版本 | 失败提交不展示 |
| 版本记录表 | 版本 | 文本 | V1、V2…… | 成功版本连续递增 |
| 版本记录表 | 状态 | 状态标签 | 固定显示已同步；最新版本可标识SAP生效 | 不展示编辑稿状态 |
| 版本记录表 | 折让后M²不含税单价 | 金额 | 该版本提交时各明细行单价 |  |
| 版本记录表 | 折让金额 | 金额 | 该版本折让计提金额合计 |  |
| 版本记录表 | 折后金额 | 金额 | 该版本折后金额合计 |  |
| 版本记录表 | 提交人 | 文本 | 成功提交人员 |  |
| 版本记录表 | 提交时间 | 日期时间 | SAP 成功返回时间 |  |
| 版本记录表 | SAP结果 | 文本 | 显示“SAP同步成功”等成功结果 | 不记录失败结果 |
| 版本记录表 | 查看完整快照 | 文字按钮 | 进入所选版本只读详情 | 操作列唯一按钮 |

### 8.4 计提单历史版本快照页

历史快照沿用计提详情布局，字段顺序和收货明细列与计提详情一致，数据全部来自所选成功版本的完整快照。

| **区域/位置** | **字段/组件名称** | **字段类型/组件类型** | **说明/功能** | **备注** |
| --- | --- | --- | --- | --- |
| 顶部 | 历史版本 Vn | 只读标签 | 明确当前查看版本 | 同时显示“历史版本，仅供查看” |
| 顶部右侧 | 返回 | 按钮 | 有编辑稿返回编辑稿，无编辑稿返回最新 SAP 成功版本 | 文案固定为“返回” |
| 头信息 | 全部计提头字段 | 只读字段 | 展示该版本提交时的头信息 | 不读取当前数据 |
| 折让计算 | 原金额、折让后M²不含税单价、折让计提金额、折后金额、备注 | 只读字段 | 展示该版本真实值 | 不按当前编辑稿单价重算 |
| 行信息 | 收货明细 | 只读表格 | 展示该版本完整收货明细及计算结果 | 不提供取数按钮 |

页面不得显示或启用：编辑、保存、删除编辑稿、删除草稿、获取最新收货记录、提交 SAP、对比 SAP、版本切换或其他修改类操作。

### 8.5 结算单管理列表页

页面用途：查询结算单，区分同步状态与对账占用状态，并进入新增、详情或编辑稿。

| **区域/位置** | **字段/组件名称** | **字段类型/组件类型** | **说明/功能** | **备注** |
| --- | --- | --- | --- | --- |
| 查询区 | 结算单号 | 输入框 | 按 S 单号查询 | 支持模糊匹配 |
| 查询区 | 结算年月 | 月份选择器 | 按结算年月筛选 |  |
| 查询区 | 计提年月 | 月份选择器 | 按关联计提年月筛选 |  |
| 查询区 | 计提单号 | 输入框 | 按关联 A 单号查询 |  |
| 查询区 | 公司代码 | 下拉框 | 按公司筛选 |  |
| 查询区 | 供应商 | 可搜索下拉框 | 按供应商筛选 |  |
| 查询区 | 同步状态 | 下拉框 | 按四种同步状态筛选 |  |
| 查询区 | 占用状态 | 下拉框 | 按未占用/已占用筛选 |  |
| 查询区 | 重置 | 按钮 | 清空条件并恢复默认列表 |  |
| 操作区 | 新增结算单 | 主按钮 | 进入新增结算单页 |  |
| 列表 | 结算单号 | 文本链接 | 展示 S 单号，点击进入详情 |  |
| 列表 | 结算年月 | 月份文本 | 展示结算业务月份 | 全局唯一 |
| 列表 | 计提单号 | 文本 | 展示自动关联的 A 单号 |  |
| 列表 | 计提年月 | 月份文本 | 展示用于关联计提单的月份 | 可与结算年月不同 |
| 列表 | 公司代码 | 文本 | 展示公司 |  |
| 列表 | 供应商 | 文本 | 展示供应商 |  |
| 列表 | 品类 | 文本 | 固定玻璃 |  |
| 列表 | 原金额 | 金额 | 展示最新已保存结算明细金额合计 | 两位小数 |
| 列表 | 折让后M²不含税单价 | 金额 | 展示最新已保存明细行单价 |  |
| 列表 | 折让计提金额/折让金额 | 金额 | 展示最新已保存折让金额合计 | 页面统一文案建议“折让金额”；接口字段为折让计提金额 |
| 列表 | 折后金额 | 金额 | 展示最新已保存折后金额合计 |  |
| 列表 | 同步状态 | 状态标签 | 草稿、已同步、待重新提交、同步失败 | 与占用状态独立 |
| 列表 | 占用状态 | 状态标签 | 未占用、已占用 | 实时联动更新 |
| 列表 | 占用对账单号 | 文本 | 已占用时展示入库对账单号 | 未占用显示“—” |
| 列表 | SAP生效版本 | 文本 | 最后一次成功版本 |  |
| 列表 | 更新时间 | 日期时间 | 最近保存或同步时间 |  |
| 列表操作 | 详情 | 文字按钮 | 进入结算单详情 | 始终可用 |
| 列表操作 | 编辑 | 文字按钮 | 进入当前编辑稿/创建编辑稿视图 | 已占用时进入后仍只读并提示原因 |
| 表格底部 | 记录数/分页 | 文本/分页器 | 展示总数并支持翻页 |  |

本页不显示“批量核对状态”、导出、批量导出或 SAP 结算状态。占用状态由入库对账单确认/作废事件实时更新，不由人工核对按钮刷新。

### 8.6 新增结算单页

页面用途：关联唯一计提单号，独立获取结算收货记录，计算并保存/提交结算单。

| **区域/位置** | **字段/组件名称** | **字段类型/组件类型** | **说明/功能** | **备注** |
| --- | --- | --- | --- | --- |
| 顶部/底部操作区 | 提交SAP | 主按钮 | 提交已保存或待首次提交的结算单 | 成功生成 V1；失败不生成版本 |
| 顶部/底部操作区 | 保存草稿 | 按钮 | 保存采购云草稿，首次保存生成 S 单号 | 不生成正式版本 |
| 顶部/底部操作区 | 返回列表 | 按钮 | 返回结算单管理 | 未保存时提示 |
| 基本信息 | 结算单号 | 只读文本 | 系统自动生成 | S+YYMMDD+3位流水号 |
| 基本信息 | 结算年月 | 月份选择器 | 当前结算业务月份 | 必填；选择与保存时校验全局唯一 |
| 基本信息 | 计提年月 | 月份选择器 | 用于查找唯一计提单号 | 必填；可与结算年月不同 |
| 基本信息 | 关联计提单 | 只读文本 | 按计提年月自动带出唯一 A 单号 | 不复制计提比例、金额或明细 |
| 基本信息 | 匹配/重新匹配 | 按钮 | 重新执行计提年月到计提单号的匹配 | 选择计提年月时系统已自动匹配；按钮用于手工重试 |
| 基本信息 | 公司代码 | 下拉框 | 选择结算公司 | 必填；参与结算收货取数 |
| 基本信息 | 供应商 | 可搜索下拉框 | 选择结算供应商 | 必填；参与取数 |
| 基本信息 | 品类 | 只读文本 | 固定玻璃 |  |
| 基本信息 | 创建日期 | 只读日期 | 单据首次生成日期 |  |
| 基本信息 | 保存日期 | 只读日期 | 最近保存日期 |  |
| 基本信息 | 货币码 | 下拉框 | 结算货币 | 必填 |
| 基本信息 | 占用状态 | 只读状态 | 新建时默认未占用 |  |
| 基本信息 | 占用对账单号 | 只读文本 | 新建时为空 |  |
| 基本信息 | SAP生效版本 | 只读文本 | 新建时为“—” |  |
| 基本信息 | 同步状态 | 只读状态 | 保存前提示待保存，保存后草稿 |  |
| 页面提示 | 年月重复提示 | 错误提示 | 展示已存在结算单号并阻断保存 |  |
| 页面提示 | 无计提单提示 | 警告提示 | 未找到计提年月对应计提单时提示先完成计提单 | 不虚构 A 单号 |
| 收货区域顶部 | 获取最新收货记录 | 按钮 | 按结算年月+公司+供应商+玻璃独立获取 | 不读取计提单明细 |
| 汇总区 | 原金额 | 只读金额 | 结算收货金额合计 |  |
| 汇总区 | 折让金额 | 只读金额 | 逐行折让计提金额合计 |  |
| 汇总区 | 折后金额 | 只读金额 | 逐行折后金额合计 |  |
| 收货明细表 | 物料凭证、行项目、入库日期 | 表格列 | 展示收货凭证信息 | 只读 |
| 收货明细表 | 采购订单、采购订单行、工厂 | 表格列 | 展示采购与工厂信息 | 采购订单行、工厂必须保留 |
| 收货明细表 | 物料编码、物料名称 | 表格列 | 展示物料信息 |  |
| 收货明细表 | 数量、单价、面积、M²不含税单价 | 数值/金额列 | 展示收货数量和价格 | 数量、面积支持负数 |
| 收货明细表 | 金额 | 金额列 | 折让计算基础 | 支持负数 |
| 收货明细表 | 折让金额 | 金额列 | 面积×折让后M²不含税单价 | 只读 |
| 收货明细表 | 折后金额 | 金额列 | 原原原原金额-折让金额 | 只读 |

结算单关联规则：选择计提年月后，只能匹配该年月唯一存在的计提单。是否允许使用该计提单，以其已完成保存且业务关系有效为前提；结算单不得继承任何计提行数据。计提年月变化时清空原计提单号和结算明细，要求重新匹配、重新取数。

### 8.7 结算单详情页

页面用途：维护结算编辑稿，查看 SAP 生效版本和历史快照，并显示占用关系。

| **区域/位置** | **字段/组件名称** | **字段类型/组件类型** | **说明/功能** | **备注** |
| --- | --- | --- | --- | --- |
| 顶部 | 结算单号 | 标题文本 | 展示当前 S 单号 |  |
| 顶部 | 同步状态 | 状态标签 | 展示四种同步状态 |  |
| 顶部 | 占用状态 | 状态标签 | 未占用/已占用 |  |
| 顶部 | 编辑稿标识 | 标签 | 有编辑稿/无编辑稿 | 不是同步状态 |
| 顶部 | 当前编辑稿 | 视图切换按钮 | 打开当前编辑稿 | 已占用时为只读冻结视图 |
| 顶部 | SAP版本 Vn | 视图切换按钮 | 打开 SAP 生效版本 | 只读 |
| 顶部操作 | 保存编辑稿 | 按钮 | 保存当前修改 | 仅未占用的编辑稿视图可用 |
| 顶部操作 | 提交SAP/重新提交SAP | 主按钮 | 提交已保存编辑稿 | 已占用、无编辑稿或有未保存修改时禁用 |
| 顶部操作 | 删除编辑稿/删除草稿 | 危险按钮 | 按第5.6节处理 | 已占用但有编辑稿时仍允许删除编辑稿 |
| 顶部操作 | 返回列表 | 按钮 | 返回结算单列表 |  |
| 页面提示 | SAP失败原因 | 错误提示 | 展示最近失败原因 | 失败不生成版本 |
| 页面提示 | 占用锁定原因 | 警告提示 | 展示占用对账单号和禁止操作原因 | 已占用时显示 |
| 版本快照/基本信息 | 当前查看版本 | 只读文本 | 当前编辑稿、SAP Vn 或历史 Vn | 编辑稿显示“基于Vn”，不预生成新版本号 |
| 版本快照/基本信息 | 版本结果 | 只读文本 | 当前视图结果 | 编辑稿显示同步状态；成功版本显示已同步 |
| 版本快照/基本信息 | 版本时间 | 只读日期时间 | 保存或同步时间 |  |
| 版本快照/基本信息 | 操作人 | 只读文本 | 保存人或提交人 |  |
| 版本快照/基本信息 | 结算年月 | 只读月份 | 当前结算月份 |  |
| 版本快照/基本信息 | 计提单号 | 只读文本 | 关联 A 单号 |  |
| 版本快照/基本信息 | 计提年月 | 只读月份 | 关联月份 | 只建立单号关系 |
| 版本快照/基本信息 | 品类 | 只读文本 | 玻璃 |  |
| 版本快照/基本信息 | 公司代码 | 只读文本 | 结算公司 |  |
| 版本快照/基本信息 | 供应商 | 只读文本 | 结算供应商 |  |
| 版本快照/基本信息 | 货币码 | 只读文本 | 结算货币 |  |
| 版本快照/基本信息 | SAP折让后M²不含税单价 | 只读金额 | 最后一次 SAP 成功版本各行单价 | 用于业务核对，不做自动差异高亮 |
| 版本快照/基本信息 | 占用对账单号 | 只读文本 | 展示引用本单的入库对账单号 | 未占用显示“未占用” |
| 汇总区 | 原金额 | 只读金额 | 当前视图原金额合计 |  |
| 汇总区 | 折让金额 | 只读金额 | 当前视图折让金额合计 |  |
| 汇总区 | 折后金额 | 只读金额 | 当前视图折后金额合计 |  |
| 行信息 | 收货明细 | 页签 | 展示当前视图的结算收货明细 | 默认页签 |
| 收货明细顶部 | 获取最新收货记录 | 按钮 | 按结算条件独立刷新并重算 | 仅未占用的编辑稿视图可用；历史/SAP视图禁用 |
| 收货明细表 | 物料凭证、行项目、入库日期 | 表格列 | 展示收货凭证信息 | 只读 |
| 收货明细表 | 采购订单、采购订单行、工厂 | 表格列 | 展示采购与工厂信息 | 只读 |
| 收货明细表 | 物料编码、物料名称 | 表格列 | 展示物料信息 | 只读 |
| 收货明细表 | 数量、单价、面积、M²不含税单价 | 数值/金额列 | 展示数量和价格 | 支持负数 |
| 收货明细表 | 金额、折让金额、折后金额 | 金额列 | 展示当前视图计算结果 |  |
| 行信息 | 版本记录 | 页签 | 展示成功正式版本 | 不通过右上角独立“历史版本”按钮打开 |
| 版本记录表 | 版本、状态、折让后M²不含税单价 | 表格列 | 展示版本基本信息 | 状态固定已同步 |
| 版本记录表 | 折让金额、折后金额 | 金额列 | 展示该版本合计 |  |
| 版本记录表 | 提交人、提交时间、SAP结果 | 表格列 | 展示成功提交记录 |  |
| 版本记录表 | 查看完整快照 | 文字按钮 | 打开所选成功版本 | 操作列唯一按钮 |

获取最新收货记录后，系统立即按各行折让后M²不含税单价重算页面金额，并将页面标记为“有未保存修改”。用户保存后：若已有 SAP 成功版本，状态变为待重新提交；若从未成功，状态保持草稿。

### 8.7.1 收货明细行级折让后M²不含税单价维护（计提单、结算单通用）

| **区域/位置** | **字段/组件名称** | **字段类型/组件类型** | **说明/功能** | **备注** |
| --- | --- | --- | --- | --- |
| 收货明细表首列 | 行选择框 | 多选框 | 勾选需要批量维护单价的明细行 | 历史快照、SAP成功版本、已占用结算单禁用 |
| 收货明细表 | 折让后M²不含税单价 | 数字输入框 | 在行上直接维护该行折后每平方米不含税单价 | 可为0或负数，四舍五入4位；编辑稿可编辑 |
| 收货明细Tab右上角 | 批量维护折让后M²不含税单价 | 按钮 | 对已勾选行一次性赋值 | 未勾选时禁用；已占用或只读视图禁用 |
| 批量维护弹窗 | 已选明细数 | 只读文本 | 显示当前勾选行数 | 至少1行才可确认 |
| 批量维护弹窗 | 折让后M²不含税单价 | 数字输入框 | 输入统一赋给所选行的单价 | 允许0或负数，最多4位小数 |
| 批量维护弹窗 | 确定 | 主按钮 | 将输入值写入所选行并逐行重算 | 关闭弹窗后页面标记为未保存 |
| 批量维护弹窗 | 取消 | 按钮 | 关闭弹窗，不改变明细 |  |


### 8.8 结算单历史版本快照页

| **区域/位置** | **字段/组件名称** | **字段类型/组件类型** | **说明/功能** | **备注** |
| --- | --- | --- | --- | --- |
| 顶部 | 历史版本 Vn | 只读标签 | 标识当前成功版本 | 同时提示“历史版本，仅供查看” |
| 顶部右侧 | 返回 | 按钮 | 有编辑稿返回当前编辑稿；无编辑稿返回最新 SAP 成功版本 | 文案固定“返回” |
| 版本快照 | 全部结算头字段 | 只读字段 | 展示该版本真实头信息和占用快照 | 占用状态可按当前业务状态另行展示，但不得改变版本金额数据 |
| 汇总区 | 原金额、折让后M²不含税单价、折让金额、折后金额 | 只读字段 | 展示该版本真实值 |  |
| 行信息 | 收货明细 | 只读表格 | 展示该版本真实明细 |  |

历史页不得显示或启用编辑、保存、删除编辑稿、获取最新收货记录、提交 SAP、对比 SAP 或历史版本按钮。

### 8.9 入库对账单详情页改造

改造位置：现有“对账开票管理—入库单明细查询详情”页面中的“入库单附加费用信息”区域。页面其他基本信息、明细信息、保存、确认、作废和普通附加费用逻辑沿用现状。

#### 8.9.1 页面字段与按钮

| **区域/位置** | **字段/组件名称** | **字段类型/组件类型** | **说明/功能** | **备注** |
| --- | --- | --- | --- | --- |
| 页面顶部 | 保存 | 按钮 | 保存当前入库对账单及附加费用 | 沿用现有功能；保存不正式占用结算单 |
| 页面顶部 | 确认 | 主按钮 | 确认入库对账单，并正式占用所选结算单 | 确认前再次校验可选条件 |
| 页面顶部 | 作废 | 危险按钮 | 作废已确认对账单并释放其占用的结算单 | 沿用现有作废权限和校验 |
| 基本信息 | 应用工厂 | 只读/下拉字段 | 当前对账单工厂 | 作为候选结算单过滤条件 |
| 基本信息 | 公司代码 | 业务上下文字段 | 当前对账单公司 | 若页面不直接展示，仍从对账单头获取 |
| 基本信息 | 供应商 | 业务上下文字段 | 当前对账单供应商 | 若页面不直接展示，仍从对账单头获取 |
| 基本信息 | 入库单号 | 只读文本 | 当前入库对账单号 | 作为占用对账单号写入结算单 |
| 基本信息 | 状态 | 状态标签 | 待确认、已确认、已作废等现有状态 | 只有待确认可新增/更换结算单 |
| 明细信息 | 现有入库明细字段 | 现有表格 | 保持现有页面逻辑 | 本次不改造 |
| 附加费用标题区 | 调整合计 | 只读金额 | 汇总附加费用行金额 | 选择结算单并回填金额后实时更新 |
| 附加费用标题区 | 新增 | 按钮 | 新增附加费用行 | 沿用现有功能 |
| 附加费用标题区 | 批量删除 | 按钮 | 删除勾选的未确认附加费用行 | 沿用现有功能；已确认引用不得以此释放占用 |
| 附加费用表 | 勾选框 | 多选框 | 用于现有批量删除 | 不是结算单候选的多选能力 |
| 附加费用表 | 序号 | 序号 | 附加费用行序号 |  |
| 附加费用表 | 调整类型 | 下拉框 | 选择附加费用类型 | 选择“折让（费用）”或“折让（物料）”时触发结算单选择框 |
| 附加费用表 | 总账科目 | 下拉/只读字段 | 按现有调整类型规则带出或选择 | 本次不改变原有科目规则 |
| 附加费用表 | 成本中心 | 下拉框 | 按现有规则选择成本中心 | 本次不改变原有规则 |
| 附加费用表 | 流水号 | 选择型输入框 | 折让类型下不允许手工录入，点击/触发时打开结算单选择框；选中后回填结算单号 | 非折让类型沿用原逻辑 |
| 附加费用表 | 查询/选择图标 | 图标按钮 | 打开结算单选择框 | 仅折让类型、待确认状态可用 |
| 附加费用表 | 税率 | 百分比输入/下拉 | 维护该附加费用行税率 | 沿用现有税率来源与精度 |
| 附加费用表 | 不含税金额 | 金额 | 选中结算单后带入其 SAP 生效版本的折让金额 | 折让类型下由系统回填，建议只读；两位小数 |
| 附加费用表 | 税额 | 只读金额 | 不含税金额×税率 | 如现有页面有该列则实时计算 |
| 附加费用表 | 含税金额 | 只读金额 | 不含税金额×（1+税率） | 两位小数，保留正负号 |
| 附加费用表 | 操作—删除 | 文字按钮 | 删除未确认附加费用行 | 对账单已确认后不可借此解除正式占用 |

#### 8.9.2 调整类型触发规则

| 调整类型 | 流水号处理 | 结算单弹窗 | 金额处理 |
| --- | --- | --- | --- |
| 折让（费用） | 结算单选择型字段 | 自动弹出，可再次点击打开 | 选中结算单后回填折让金额并计算含税金额 |
| 折让（物料） | 结算单选择型字段 | 自动弹出，可再次点击打开 | 选中结算单后回填折让金额并计算含税金额 |
| 其他调整类型 | 沿用原页面规则 | 不打开 | 沿用原金额逻辑 |

用户从折让类型改为其他类型时，系统清空该行的结算单关联、流水号和由结算单自动带入的金额，再按新类型执行原有逻辑。用户在两个折让类型之间切换时可保留已选结算单，但保存/确认前仍需重新校验候选条件。

#### 8.9.3 选择结算单弹出框

弹窗标题固定为“选择结算单”。列表字段与结算单管理列表保持一致；在弹窗中增加单选列，禁止多选。

| **区域/位置** | **字段/组件名称** | **字段类型/组件类型** | **说明/功能** | **备注** |
| --- | --- | --- | --- | --- |
| 弹窗顶部 | 匹配条件提示 | 提示文本 | 展示当前公司、供应商、工厂，以及“已同步、未占用”条件 | 便于用户确认筛选口径 |
| 候选列表 | 单选 | 单选框 | 选择一张结算单 | 只能单选 |
| 候选列表 | 结算单号 | 文本 | 展示 S 单号 | 选中后回填流水号 |
| 候选列表 | 结算年月 | 月份 | 展示结算月份 |  |
| 候选列表 | 计提单号 | 文本 | 展示关联 A 单号 |  |
| 候选列表 | 计提年月 | 月份 | 展示关联计提月份 |  |
| 候选列表 | 公司代码 | 文本 | 展示公司 | 必须与当前对账单公司一致 |
| 候选列表 | 供应商 | 文本 | 展示供应商 | 必须与当前对账单供应商一致 |
| 候选列表 | 品类 | 文本 | 展示玻璃 |  |
| 候选列表 | 原金额 | 金额 | 展示结算原金额合计 | 两位小数 |
| 候选列表 | 折让后M²不含税单价 | 金额 | 展示当前 SAP 生效版本各明细行单价 |  |
| 候选列表 | 折让金额 | 金额 | 展示当前 SAP 生效版本折让金额 | 选中后回填不含税金额 |
| 候选列表 | 折后金额 | 金额 | 展示当前 SAP 生效版本折后金额 |  |
| 候选列表 | 同步状态 | 状态标签 | 候选数据固定已同步 | 其他状态不进入结果集 |
| 候选列表 | 占用状态 | 状态标签 | 候选数据固定未占用 | 已占用不进入结果集 |
| 候选列表 | 占用对账单号 | 文本 | 候选数据应为空 | 保持与结算列表字段一致 |
| 候选列表 | SAP生效版本 | 文本 | 展示被引用的成功版本 | 对账单确认时固化引用版本 |
| 候选列表 | 更新时间 | 日期时间 | 展示结算单最近更新时间 |  |
| 弹窗底部 | 确定 | 主按钮 | 将当前单选结算单带回附加费用行 | 未选择时禁用 |
| 弹窗底部 | 取消/关闭 | 按钮 | 关闭弹窗且不改变原选择 |  |

候选查询由系统自动使用当前入库对账单的公司、供应商、应用工厂，用户不可改成其他主体。弹窗业务字段与结算单管理列表一致，不额外增加工厂列；工厂条件由系统后台过滤。结算单工厂以其收货明细中的工厂为准；若一张结算单包含多个工厂，只要其被引用折让金额能够按当前工厂明确归属才可入选，否则不得出现在候选列表中，避免跨工厂带入整单金额。

#### 8.9.4 回填与占用时点

| 时点 | 流水号 | 不含税金额/含税金额 | 结算单占用状态 |
| --- | --- | --- | --- |
| 用户在弹窗点击确定 | 回填结算单号 | 回填折让金额并按税率计算 | 仍为未占用，仅形成当前页面临时选择 |
| 保存待确认对账单 | 保存所选结算单号 | 保存当前计算金额 | 不正式占用；其他业务如需避免并发，以确认时最终校验为准 |
| 确认对账单 | 固化引用 | 固化金额和引用 SAP 版本 | 更新为已占用，写入当前入库对账单号 |
| 作废已确认对账单 | 保留作废历史引用 | 保留作废单历史金额 | 释放为未占用，清空结算单当前占用对账单号 |

确认时若结算单已被其他对账单先占用、同步状态已变化或公司/供应商/工厂不再匹配，系统阻断确认并提示重新选择。

## 9 页面显隐与可编辑规则

### 9.1 计提单详情状态矩阵

| 视图/状态 | 折让后M²不含税单价与备注 | 获取最新收货记录 | 保存 | 提交SAP | 删除 | 版本记录 | 返回 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 首次草稿 | 可编辑 | 可用 | 可用 | 已保存后可用；直接提交时系统先保存 | 显示“删除草稿”，删除整单 | 无成功版本时为空 | 返回列表 |
| 已同步且无编辑稿—SAP视图 | 只读 | 不显示/禁用 | 不显示/禁用 | 不显示/禁用 | 不显示 | 可查看 | 返回列表 |
| 已同步并进入新编辑稿 | 可编辑 | 可用 | 有修改时可用 | 保存后可用 | 显示“删除编辑稿” | 可查看历史成功版本 | 返回列表 |
| 待重新提交 | 可编辑 | 可用 | 有修改时可用 | 可用 | 显示“删除编辑稿” | 可查看 | 返回列表 |
| 同步失败 | 可编辑 | 可用 | 修改后可用 | 可再次提交 | 显示“删除编辑稿/删除草稿” | 只展示成功版本 | 返回列表 |
| SAP生效版本视图 | 全部只读 | 不显示/禁用 | 不显示/禁用 | 不显示/禁用 | 不显示 | 可查看 | 返回列表 |
| 历史版本快照 | 全部只读 | 不显示 | 不显示 | 不显示 | 不显示 | 不显示其他操作 | 按有无编辑稿返回 |

### 9.2 结算单详情状态矩阵

| 视图/状态 | 折让后M²不含税单价 | 获取最新收货记录 | 保存 | 提交SAP | 删除编辑稿 | 版本记录 | 说明 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 首次草稿、未占用 | 可编辑 | 可用 | 可用 | 已保存后可用 | 删除整张草稿 | 无成功版本时为空 | 正常创建流程 |
| 已同步无编辑稿、未占用 | SAP视图只读；进入编辑稿后可编辑 | 编辑稿可用 | 编辑稿有修改时可用 | 保存后可用 | 有编辑稿时显示 | 可查看 |  |
| 待重新提交、未占用 | 可编辑 | 可用 | 有修改时可用 | 可用 | 可用 | 可查看 |  |
| 同步失败、未占用 | 可编辑 | 可用 | 修改后可用 | 可再次提交 | 可用 | 只展示成功版本 |  |
| 已占用、无编辑稿 | 只读 | 禁用 | 禁用 | 禁用 | 不显示 | 可查看 | 显示占用对账单号 |
| 已占用、有编辑稿 | 编辑稿冻结只读 | 禁用 | 禁用 | 禁用 | 可用 | 可查看 | 删除后恢复 SAP 版本，但仍保持占用只读 |
| SAP生效版本视图 | 只读 | 禁用 | 禁用 | 禁用 | 不显示 | 可查看 |  |
| 历史版本快照 | 只读 | 不显示 | 不显示 | 不显示 | 不显示 | 不显示其他修改操作 | 右上角仅保留返回 |

### 9.3 入库对账单附加费用状态矩阵

| 对账单状态 | 调整类型可编辑 | 可选择/更换结算单 | 金额回填 | 正式占用 | 作废释放 |
| --- | --- | --- | --- | --- | --- |
| 待确认 | 是 | 是，仅折让两种类型 | 选择后实时回填 | 否 | 不适用 |
| 确认处理中 | 否 | 否 | 按保存值校验 | 原子校验并占用 | 不适用 |
| 已确认 | 否 | 否 | 只读 | 是 | 可按现有作废权限执行 |
| 已作废 | 否 | 否 | 保留历史只读 | 否，已释放 | 已完成 |

## 10 SAP 接口数据范围

Excel 中实际使用 H 表示头字段、I 表示行明细字段。页面不展示 H/I 标识。本节只说明数据范围，不定义技术字段编码或报文结构。

### 10.1 计提单头字段

| **区域/位置** | **字段/组件名称** | **字段类型/组件类型** | **说明/功能** | **备注** |
| --- | --- | --- | --- | --- |
| SAP计提头 | 公司代码 | 文本 | 计提所属公司 | Excel标识H，KEY |
| SAP计提头 | 计提单号 | 文本 | 采购云计提业务单号 | Excel标识H，KEY |
| SAP计提头 | 计提年月 | 月份 | 计提所属年月 | Excel标识H，KEY |
| SAP计提头 | 供应商 | 文本 | 计提供应商 | Excel标识H，KEY |
| SAP计提头 | 创建日期 | 日期 | 单据创建日期 | Excel标识H |
| SAP计提头 | 保存日期 | 日期 | 最近保存日期 | Excel标识H |
| SAP计提头 | 货币码 | 文本 | 金额货币 | Excel标识H |

采购云页面额外维护品类、折让后M²不含税单价、同步状态、SAP生效版本、备注、修改人与时间等业务字段。它们是否进入 SAP 报文，以接口最终定义为准；其中折让计算结果通过行字段传输。

### 10.2 计提单行字段

| **区域/位置** | **字段/组件名称** | **字段类型/组件类型** | **说明/功能** | **备注** |
| --- | --- | --- | --- | --- |
| SAP计提行 | 物料凭证 | 文本 | 收货物料凭证号 | Excel标识I，KEY |
| SAP计提行 | 行项目 | 文本 | 凭证行号 | Excel标识I，KEY |
| SAP计提行 | 入库日期 | 日期 | 实际入库日期 | Excel标识I |
| SAP计提行 | 采购订单 | 文本 | 采购订单号 | Excel标识I |
| SAP计提行 | 采购订单行 | 文本 | 采购订单行号 | Excel标识I |
| SAP计提行 | 工厂 | 文本 | 收货工厂 | Excel标识I |
| SAP计提行 | 物料 | 文本 | 物料编码 | Excel标识I |
| SAP计提行 | 数量 | 数值 | 收货/退货数量 | Excel标识I，考虑正负数 |
| SAP计提行 | 单价 | 金额 | 业务单价 | Excel标识I |
| SAP计提行 | 面积 | 数值 | 玻璃面积 | Excel标识I |
| SAP计提行 | M²不含税单价 | 金额 | 每平方米不含税价格 | Excel标识I |
| SAP计提行 | 金额 | 金额 | 折让计算基础金额 | Excel标识I |
| SAP计提行 | 折后金额 | 金额 | 金额减折让计提金额 | Excel标识I |
| SAP计提行 | 折让计提金额 | 金额 | 金额乘折让后M²不含税单价 | Excel标识I |

### 10.3 结算单头字段

| **区域/位置** | **字段/组件名称** | **字段类型/组件类型** | **说明/功能** | **备注** |
| --- | --- | --- | --- | --- |
| SAP结算头 | 公司代码 | 文本 | 结算所属公司 | Excel标识H，KEY |
| SAP结算头 | 结算单号 | 文本 | 采购云结算业务单号 | Excel标识H，KEY |
| SAP结算头 | 结算年月 | 月份 | 结算业务月份 | Excel标识H，KEY |
| 采购云关联 | 计提单号 | 文本 | 通过计提年月自动关联 | Excel中未标H/I；采购云必须保存 |
| 采购云关联 | 计提年月 | 月份 | 用于关联计提单号 | Excel中未标H/I；采购云必须保存 |
| SAP结算头 | 供应商 | 文本 | 结算供应商 | Excel标识H，KEY |
| SAP结算头 | 创建日期 | 日期 | 单据创建日期 | Excel标识H |
| SAP结算头 | 保存日期 | 日期 | 最近保存日期 | Excel标识H |
| SAP结算头 | 货币码 | 文本 | 金额货币 | Excel标识H |

品类、折让后M²不含税单价、同步状态、占用状态、占用对账单号和 SAP 生效版本为采购云业务字段。占用信息不随折让同步接口传输，由采购云对账联动维护。

### 10.4 结算单行字段

| **区域/位置** | **字段/组件名称** | **字段类型/组件类型** | **说明/功能** | **备注** |
| --- | --- | --- | --- | --- |
| SAP结算行 | 物料凭证 | 文本 | 结算收货物料凭证 | Excel标识I，KEY |
| SAP结算行 | 行项目 | 文本 | 凭证行号 | Excel标识I，KEY |
| SAP结算行 | 入库日期 | 日期 | 实际入库日期 | Excel标识I |
| SAP结算行 | 采购订单 | 文本 | 采购订单号 | Excel标识I |
| SAP结算行 | 采购订单行 | 文本 | 采购订单行号 | Excel标识I |
| SAP结算行 | 工厂 | 文本 | 收货工厂 | Excel标识I |
| SAP结算行 | 物料 | 文本 | 物料编码 | Excel标识I |
| SAP结算行 | 数量 | 数值 | 收货/退货数量 | Excel标识I，考虑正负数 |
| SAP结算行 | 单价 | 金额 | 业务单价 | Excel标识I |
| SAP结算行 | 面积 | 数值 | 玻璃面积 | Excel标识I |
| SAP结算行 | M²不含税单价 | 金额 | 每平方米不含税价格 | Excel标识I |
| SAP结算行 | 金额 | 金额 | 折让计算基础金额 | Excel标识I |
| SAP结算行 | 折后金额 | 金额 | 金额减折让计提金额 | Excel标识I |
| SAP结算行 | 折让计提金额 | 金额 | 金额乘折让后M²不含税单价 | Excel标识I |

## 11 校验与异常提示

| 场景 | 系统处理 | 建议提示 |
| --- | --- | --- |
| 计提年月重复 | 阻断保存和提交，不生成单号 | “2026-09 已存在计提单 A260916001，每个计提年月只能创建一张计提单，请直接进入原单据查看或修改。” |
| 结算年月重复 | 阻断保存和提交，不生成单号 | “2026-11 已存在结算单 S261105001，每个结算年月只能创建一张结算单，请直接进入原单据查看或修改。” |
| 结算未找到计提单 | 清空关联计提单，不允许保存 | “未找到2026-09对应的计提单，请先完成该月份计提单。” |
| 关键取数条件缺失 | 不调用收货查询 | “请先补全年月、公司代码和供应商。” |
| 未获取收货记录 | 阻断保存/提交 | “请先获取符合当前条件的收货记录。” |
| 折让后M²不含税单价为空或越界 | 阻断保存/提交 | “请维护每条收货明细的折让后M²不含税单价（允许为0或负数，最多4位小数）。” |
| 有未保存修改直接提交 | 阻断提交 | “存在未保存修改，请先保存编辑稿。” |
| SAP接口失败 | 保留编辑稿，不生成版本 | 展示 SAP 原始明确原因；无明确原因时显示“SAP接口异常，请稍后重试。” |
| SAP业务已完成结算 | 保留编辑稿，原版本不变 | “同步失败：该业务数据已在SAP完成结算，不允许修改。” |
| 已占用结算单执行修改类操作 | 阻断操作 | “该结算单已被对账单{单号}占用，不允许修改或重新提交。” |
| 对账候选为空 | 弹窗展示空状态 | “未找到与当前公司、供应商、工厂匹配且已同步、未占用的结算单。” |
| 对账确认时结算单被抢占 | 阻断确认，清除失效选择或要求重选 | “所选结算单已被其他对账单占用，请重新选择。” |
| 历史快照尝试修改 | 不提供操作入口 | “历史版本仅供查看。” |

## 12 边界与并发规则

- 月份唯一性在前端选择时和服务端保存/提交时均校验，服务端结果为最终依据。
- 单号流水在服务端并发生成，任何客户端不得自行拼接最终流水号。
- SAP 提交应防止重复点击；同一次提交只处理一次成功或失败结果。
- 用户打开详情后，若单据已被其他用户更新，保存时应提示刷新最新数据，不能静默覆盖。
- 入库对账单确认占用必须做最终并发校验；候选弹窗中“未占用”不代表确认时仍可占用。
- 对账单作废只释放由本对账单占用的结算单；若占用对账单号不一致，不得误释放。
- 一张已确认入库对账单不得通过删除附加费用行、修改流水号或改调整类型绕过作废流程释放结算单。
- 历史快照必须持久化完整行数据。后续重新获取收货记录不得影响任何历史成功版本。
- 负数行的折让计提金额和折后金额按公式自然计算并保留负号。
- 若同一结算单明细跨多个工厂且无法拆分当前工厂对应的折让金额，该结算单不进入该工厂入库对账单候选列表。

## 13 验收场景

| 编号 | 验收场景 | 操作 | 预期结果 |
| --- | --- | --- | --- |
| AT-01 | 计提年月唯一 | 新建时选择已有月份 | 即时提示已有 A 单号；保存/提交均被阻断 |
| AT-02 | 新建计提单 | 取数、填比例、保存、提交 | 保存生成 A 单号和草稿；SAP 成功生成 V1并变为已同步 |
| AT-03 | 计提修改未保存 | 在已同步详情修改比例但不保存 | 详情显示“有未保存修改”；列表仍显示原已保存比例和状态 |
| AT-04 | 计提重新提交成功 | 保存修改并提交 | 保存后待重新提交；成功生成下一版本并进入版本记录 |
| AT-05 | 计提重新提交失败 | SAP 返回业务已结算 | 状态同步失败；编辑稿保留；原 SAP 版本不变；无失败版本记录 |
| AT-06 | 计提历史返回 | 从 V1 快照点击返回 | 有编辑稿返回编辑稿；无编辑稿返回最新 SAP 版本 |
| AT-07 | 结算年月唯一 | 新建时选择已有月份 | 提示已有 S 单号并阻断保存 |
| AT-08 | 结算关联计提 | 选择计提年月 | 自动带出唯一计提单号，不带出计提明细、比例或金额 |
| AT-09 | 结算独立取数 | 点击获取最新收货记录 | 按结算年月+公司+供应商+玻璃取得结算明细并重算 |
| AT-10 | 结算版本记录 | 连续两次成功提交，中间一次失败 | 只显示 V1、V2；失败不生成或占用 V2/V3 |
| AT-11 | 已占用结算单 | 打开已占用结算单 | 修改、取数、保存、提交全部禁用；历史可看 |
| AT-12 | 已占用且有编辑稿 | 点击删除编辑稿 | 恢复最后 SAP 版本；仍为已占用且只读 |
| AT-13 | 折让类型触发弹窗 | 附加费用选择折让（费用）或折让（物料） | 自动打开“选择结算单”弹窗 |
| AT-14 | 候选过滤 | 打开结算单弹窗 | 只展示公司、供应商、工厂匹配且已同步、未占用的结算单 |
| AT-15 | 单选回填 | 单选结算单并确定 | 结算单号带入流水号；折让金额带入不含税金额；含税金额按税率计算 |
| AT-16 | 并发占用 | 两张对账单先后确认同一结算单 | 第一张成功；第二张确认时被阻断并要求重选 |
| AT-17 | 对账作废释放 | 作废已确认入库对账单 | 结算单恢复未占用，占用对账单号清空，可被其他对账单选择 |
| AT-18 | 路由与控制台 | 逐一打开所有列表、详情、历史页和弹窗 | 无白屏、无明显 JavaScript 报错、无无效按钮事件 |

## 14 开发交付检查清单

- 计提单、结算单页面按全新功能交付；入库对账单仅改造现有附加费用区域。
- 所有页面、筛选、Mock/测试数据和判断逻辑均不包含 SAP 结算状态。
- 两类单据只使用草稿、已同步、待重新提交、同步失败四种同步状态。
- 编辑稿保存不生成正式版本；失败提交不生成版本、不进入版本记录。
- 列表展示采购云最新已保存内容，不混入未保存页面值。
- 计提和结算详情均无 SAP 版本对比功能。
- 版本记录操作列只有“查看完整快照”。
- 历史快照右上角有“返回”，并按有无编辑稿返回正确目标。
- 结算收货记录独立获取，不复制计提单明细。
- 已占用结算单的修改、取数、保存、提交均被拦截；删除编辑稿例外。
- 入库对账附加费用的两种折让类型均能打开结算单单选弹窗。
- 候选结算单严格按公司、供应商、工厂、已同步和未占用过滤。
- 结算单号、折让金额、不含税金额、税率、含税金额的回填与计算正确。
- 对账确认占用和作废释放均有服务端最终校验，列表状态实时联动。
